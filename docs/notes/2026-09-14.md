# Working notes: 2026-09-14

## Selection

- Seed: `20260914`
- Python `random.Random` mode roll: `0.4117408971314923`
- Mode: new project
- Previous four scheduled lanes: games, software development and engineering, Android, Android
- Eligible new-project lanes after exclusion: data science and iOS
- Uniform eligible-list index: `1`
- Selected lane: iOS applications

## Portfolio inspection

The workspace contained recent Android tools for touch ergonomics and frame pacing, older generic Flutter examples, data/ML tools, browser games, and software-engineering utilities. The visible public GitHub inventory did not contain a native Swift or photo-metadata privacy project.

ExifVeil differs in its central problem, consumer privacy user, image-file metadata modality, ImageIO/Core Image processing, SwiftUI interface, and non-destructive picker/share boundary. It is not an ECE project.

## Implementation observations

- Broad Photos authorization is unnecessary because `PhotosPicker` can provide the selected item.
- Exact GPS values should be detected but hidden from the app report to avoid echoing sensitive coordinates on screen.
- Sanitization needs an output audit, not only a successful encoder return value.
- Removing EXIF orientation without transforming pixels can rotate the result. The implementation therefore orients decoded pixels before encoding.
- The Windows host has no Swift, Xcode, Dart, or Flutter tooling. Native compilation and simulator tests must run in public macOS CI; local verification is limited to structure, privacy-boundary assertions, documentation, and repository checks.

## Verification outcome

- Local Node verifier passed 8 checks; README scans found 0 em dash characters and the SVG parsed as XML.
- Hosted macOS compiled the core and passed all 6 Swift package tests.
- XcodeGen created the project, Xcode compiled the SwiftUI target, and the same 6 tests passed in an iOS Simulator.
- The generated metadata-bearing fixture reached all five categories and a bounded signal of 100. The sanitized output had 0 known sensitive findings.
- CI found and drove three concrete portability corrections: reducer syntax, zero-frame ImageIO behavior, and conditional module imports for the shared test suite.
- Passing native run: `34891290417`.
